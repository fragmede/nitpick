package storyview

// This file is intentionally minimal.
// Comment rendering is done directly in model.go's rebuildContent()
// using a viewport rather than a list delegate, since comments have
// variable heights and need indentation + depth coloring.
